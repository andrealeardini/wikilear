{% extends "layouts/base.njk" %}
{% block content %}
   <article class="{{ style }} mx-auto">
    {%if not hide_title %}<h1>{{title}}</h1>{%endif %}
    {{ content | safe }}
  </article>
{% endblock %}
